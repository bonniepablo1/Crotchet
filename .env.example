# Supabase Configuration
# Copy this file to .env and fill in your actual values

# Supabase Project URL
# Find this in: Supabase Dashboard > Settings > API > Project URL
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anonymous Key (Public)
# Find this in: Supabase Dashboard > Settings > API > Project API keys > anon/public
# This key is safe to use in client-side code
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Supabase Service Role Key (Server-side only)
# Find this in: Supabase Dashboard > Settings > API > Project API keys > service_role
# ⚠️  WARNING: Never expose this key in client-side code or commit it to version control
# Only use this for server-side operations or migrations
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional: For CLI operations (not needed for app runtime)
# SUPABASE_PROJECT_REF=your-project-ref
# SUPABASE_ACCESS_TOKEN=your-access-token
# SUPABASE_PROJECT_REF_STAGING=your-staging-project-ref
